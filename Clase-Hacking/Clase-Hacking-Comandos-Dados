Clase Hacking 30-10-2024
  ping
  arp-scan
  Nmap -vv -sV -sC -O -p 445
  cd /usr/share/nmap/scripts para ver todos los scripts de nmap

Clase Hacking 31-10-2024 <----Como evadir un firewall---->
  NmapFirewallEvasion <---most comon comnands--->
    TOP EVASION
      --source-port 
       
      -D
       
      -spoof-mac
       
      -sS
       
      --min-rate
   
  --mtu --> minimo 8 + la suma del minimo de nmap = 20


savitar nmap -p- --open -sS --min-rate 5000 -vvv -n -Pn <IP>
        nmap -sCV -p80,443,21 <IP>

  --data-leght: longitud de los paquetes.  

  -f (fragmented): fragmentar los paquetes. 

  --source-port: te haces pasar por el puerto 80; y si la empresa bloquea el puerto 80, no tiene página web. 

  -D (Decoy): Para enviar paquetes falsos. Hay que poner direcciones ips falsas. 

  -spoof-mac: Cambiar la MAC de origen. 

  -sS: SYN-Scan: hace sólo el SYN del Three Way HandShake 

  --min-rate: mínimo de paquetes por segundo.

  Three way handshake: SYN, SYN-ACK, ACK, TCP.

  RST: la terminación de una conexión TCP. 

  smbclient es parecido a crackmapexec 

  Cuando haces el smbclient, las carpetas con $ son carpetas predefinidas que se crean por defecto. (gestión, mantenimiento). 
  enum4linux es para enumerar usuarios. (smbclient no saca los usuarios, pero si las carpetas). También es 445
  snmpwalk: lista los dispositivos disponibles en una red.(que puedas interactuar con ellos). 161 y 162 por UDP 

  arp-scan(alternativa para snmpwalk) 

  Para decodear es con base64 --decode 

  echo "Qm9iIC0gIVBAJCRXMHJEITEyMw" | base64 --decode 

  LDAP lo usa Active Directory para que los equipos se comuniques entre si. 

  nbtscan: lista los equipos que hay en la red por NetBIOS(es un protocolo de Windows que 
  viene por defecto para que los equipos se comuniquen con la red, ahora se utiliza el 
  smb). El puerto 139 es para el nbtscan por TCP. 

  snmpwalk, nbtscan y arp-scan tienen sus diferencias. 
  arp-scan se utiliza siempre, no depende de puertos. 









